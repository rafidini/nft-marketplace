import { Profile } from "..";
import { OAuthConfig, OAuthUserConfig } from "./oauth";
export interface GoogleProfile extends Profile {
    sub: string;
    picture: string;
}
export default function Google<P extends GoogleProfile>(options: OAuthUserConfig): OAuthConfig<P>;
